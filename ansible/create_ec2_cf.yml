# Instance creation
# Run : ansible-playbook -vvv -i ansible/env/hosts -e env_type=nonprod ansible/create_ec2_cf.yml
- hosts: localhost
  connection: local
  roles:
    - ec2-cf
    - { role: ec2, "vars_file": "group_vars/jenkins.{{env_type}}.yml" }
  tags: ec2_create